"""Small calculator module used by the Coding Agent demo."""


def divide(dividend: float, divisor: float) -> float | None:
    """Return the quotient, or None when the divisor is zero."""
    return dividend / divisor
